import 'package:flutter_test/flutter_test.dart';

void main() {
  group('UserDataService', () {
    test('loadUserData returns null when user not logged in', () async {
      // This is a placeholder test
      // Full tests require Firebase mocking (mockito/fake_cloud_firestore)
      expect(true, true);
    });

    test('getUserActiveRoom returns null when user has no active room', () async {
      // Placeholder test
      expect(true, true);
    });

    test('getUserActiveRoom returns roomId when user has active room', () async {
      // Placeholder test
      expect(true, true);
    });
  });
}
